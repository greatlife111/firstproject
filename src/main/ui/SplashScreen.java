package ui;

import java.awt.*;

public class SplashScreen extends Frame {
    static void renderSplashFrame(Graphics2D image, int frame) {
        final String[] composition = {"oh", "yes"};
        image.setComposite(AlphaComposite.Clear);
    }
}
